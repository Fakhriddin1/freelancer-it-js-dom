// Получаем объект 
const lessonBlock = document.querySelector('.lesson');

// получаем объект
const title = document.querySelector('h3');

// перености title в конец блока lessonBlock
// lessonBlock.append(title)